apiVersion: v1
kind: Service
metadata:
  name: {{ include "pass-transfer.fullname" . }}
  labels: {{ include "pass-transfer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "pass-transfer.selectorLabels" . | nindent 4 }}
